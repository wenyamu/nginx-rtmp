# linux命令行启动摄像头拍照,从终端拍照

## v4l2-ctl 是 ‌v4l-utils‌ 软件包的一部分，先安装
```
apt-get install v4l-utils
```
## 查看已经连接的设备
```
root@orangepizero3:~# v4l2-ctl --list-devices
EMEET SmartCam C60E 4K: EMEET S (usb-sunxi-ehci-1):
	/dev/video0
```

## 打印出/dev/video0设备的详细信息
```
root@orangepizero3:~# v4l2-ctl -d /dev/video0 --all
```

## 这个命令会运行一系列的测试，以确定设备是否支持V4L2
```
root@orangepizero3:~# v4l2-compliance -d /dev/video0
```

## 查看摄像头支持的格式、支持的分辨率和帧率
```
root@orangepizero3:~# v4l2-ctl -d /dev/video0 --list-formats-ext
ioctl: VIDIOC_ENUM_FMT
	Type: Video Capture

	[0]: 'MJPG' (Motion-JPEG, compressed)
		Size: Discrete 3840x2160
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 2560x1440
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 1920x1080
			Interval: Discrete 0.017s (60.000 fps)
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 1280x960
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 1280x720
			Interval: Discrete 0.017s (60.000 fps)
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 1024x576
			Interval: Discrete 0.017s (60.000 fps)
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 960x720
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 800x600
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 640x480
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 640x360
			Interval: Discrete 0.017s (60.000 fps)
			Interval: Discrete 0.033s (30.000 fps)
	[1]: 'YUYV' (YUYV 4:2:2)
		Size: Discrete 640x480
			Interval: Discrete 0.033s (30.000 fps)
		Size: Discrete 640x360
			Interval: Discrete 0.033s (30.000 fps)

```

# 1，安装 fswebcam
```
apt install -y fswebcam
```

## 使用 fswebcam 拍照
```
fswebcam -q -d /dev/video0 --no-banner -r 3840x2160 -D 1 --jpeg 100 --save /root/test/fs.jpg

### -q             静默，除非有错误才会打印信息
### -d /dev/video0 指定设备
### --no-banner    图片不添加水印
### -r 3840x2160   使用摄像头的分辨率，这里是4K
### -D 1           延迟1s拍照
### --jpeg 100     设置图片质量
### --save /root/test/fs.jpg   保存图片位置(--save参数名可以省略不写，最后只跟路径也可以)
```

## 使用 fswebcam 循环拍照
```
fswebcam -q -d /dev/video0 --no-banner -r 3840x2160 -l 3 --jpeg 100 --save /root/test/fs-%Y%m%d-%H%M%S.jpg

### -q             静默，除非有错误才会打印信息
### -d /dev/video0 指定设备
### --no-banner    图片不添加水印
### -r 3840x2160   使用摄像头的分辨率，这里是4K
### -l 3            参数 --loop <秒> 表示每3秒执行一次，一直循环下去。ctrl+c结束执行
### --jpeg 100     设置图片质量
### --save /root/test/fs-%Y%m%d-%H%M%S.jpg   保存图片位置(--save可以省略不写，最后只跟路径也可以)
```

# 2，安装ffmpeg
```
apt install -y ffmpeg
```

## 使用 ffmpeg 拍照

```
ffmpeg -y -f video4linux2 -input_format mjpeg -video_size 3840x2160 -i /dev/video0 -ss 0:0:2 -frames 1 /root/test/ffmpeg-img.jpg

### -y 覆盖已经存在的文件
### -f video4linux2 指定输入设备采用V4L2（Video4Linux）驱动, 也可以简写为 v4l2
### -input_format mjpeg 通过 `v4l2-ctl -d /dev/video0 --list-formats-ext` 得到 MJPG，注意这里参数值必须是 mjpeg 并且还必须是小写
### -video_size 3840x2160 指定摄像头支持的分辨率，这里使用的是 4K
### -i /dev/video0  指定设备
### -ss 00:00:02    截取的具体时间点(00:00:02 表示从2s处截取)
### -frames 1       捕获1帧
### /root/test/ffmpeg-img.jpg    保存图片路径
```

## 使用 ffmpeg 录像
```
ffmpeg -y -f v4l2 -input_format mjpeg -video_size 3840x2160 -framerate 30 -i /dev/video0 \
-c:v libx264 -preset ultrafast -r 30 -tune zerolatency -pix_fmt yuv420p \
-t 30 /root/test/ffmpeg-video.mp4

### -y 覆盖已经存在的文件
### -f v4l2         指定输入设备采用V4L2（Video4Linux）驱动, 也可以简写为 v4l2
### -input_format mjpeg 通过 `v4l2-ctl -d /dev/video0 --list-formats-ext` 得到 MJPG，注意这里参数值必须是 mjpeg 并且还必须是小写
### -video_size 3840x2160 指定摄像头支持的分辨率，这里使用的是 4K
### -framerate 30   指定帧率为30帧/秒
### -i /dev/video0  指定设备
### -t 30                     表示录像时长（秒），如果不加此参数会一直录制下去。ctrl+c结束录制
### /root/test/ffmpeg-video.mp4   保存路径
```

## 使用 ffmpeg 录像, 测试过很流畅，无声音
```
ffmpeg -y \
-f v4l2 \
-input_format mjpeg \
-video_size 1920x1080 \
-framerate 30 \
-i /dev/video0 \
-c:v libx264 \
-preset ultrafast \
-tune zerolatency \
-b:v 8M \
-minrate 6M \
-maxrate 10M \
-bufsize 20M \
-buffer_size 20M \
-pix_fmt yuv420p \
-r 30 \
-t 10 \
/root/test/ffmpeg-video.mp4
```



# 录制usb摄像头的音频
## 先查找usb摄像头的音频设备号，其中 card 3, device 0 就是对应摄像头的型号及音频设备号
```
root@orangepizero3:~# arecord -l
**** List of CAPTURE Hardware Devices ****
card 2: ahubhdmi [ahubhdmi], device 0: ahub_plat-snd-soc-dummy-dai snd-soc-dummy-dai-0 []
  Subdevices: 1/1
  Subdevice #0: subdevice #0
card 3: E4K [EMEET SmartCam C60E 4K], device 0: USB Audio [USB Audio]
  Subdevices: 1/1
  Subdevice #0: subdevice #0
```

## 录制音频
```
arecord -D hw:3,0 -r 48000 -c 2 -f S16_LE -d 5 test_mic_48k.wav

### -D hw:3,0 -D 代表 --device，hw:3,0 声卡编号3,该声卡下的‌设备编号0, 就是 `arecord -l` 获取的 "card 3, device 0"
### -r 48000  -r 代表 --rate， 表示每秒采集 48,000 个音频样本
### -c 2      -c 代表 --channels， 表示录制‌‌双声道（立体声），为1时表示单声道
### -f S16_LE -f 代表 --format， 表示采样格式
### -d 5      -d 代表 --duration，表示录制 ‌5 秒‌后自动停止

```


# 录制带有音频的视频

## 执行命令查看 ALSA 支持状态
```
root@orangepizero3:~# ffmpeg -formats | grep alsa
ffmpeg version 7.0.2-static https://johnvansickle.com/ffmpeg/  Copyright (c) 2000-2024 the FFmpeg developers
  built with gcc 8 (Debian 8.3.0-6)
  ......
DEd alsa            ALSA audio output
```

## 注意 ffmpeg 要支持 alsa, 并且我的香橙派性能问题，最大只能支持 1280x720，再大分辨率就会出现声音断断续续的
```
ffmpeg -y \
-f v4l2 -input_format mjpeg -video_size 1280x720 -framerate 30 -i /dev/video0 \
-f alsa -ar 48000 -ac 2 -i hw:3,0 \
-c:v libx264 \
-preset ultrafast \
-tune zerolatency \
-b:v 8M \
-minrate 6M \
-maxrate 10M \
-bufsize 20M \
-buffer_size 20M \
-pix_fmt yuv420p \
-r 30 \
-t 10 \
/root/test/ffmpeg-video-audio2.mp4
```
